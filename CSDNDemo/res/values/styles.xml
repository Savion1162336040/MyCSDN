<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="blackHDcutoff1dp">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/light_black</item>
    </style>

    <style name="grayHDcutoff1dp">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:alpha">0.2</item>
        <item name="android:background">@color/gray_text_color</item>
    </style>
    
    <style name="grayVDcutoff1dp">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:alpha">0.2</item>
        <item name="android:background">@color/gray_text_color</item>
    </style>

    <style name="anim_activity" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:activityOpenEnterAnimation">@anim/right_in</item>
        <item name="android:activityCloseExitAnimation">@anim/right_out</item>
        <item name="android:activityOpenExitAnimation">@anim/left_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/left_in</item>
    </style>

</resources>